const content = [
  {
    type: 'paragraph',
    children: [{ text: 'A line of text in a paragraph.' }]
  }
]

const getAboutOrganisation = organisationId => {
  return content
}

export { getAboutOrganisation }
